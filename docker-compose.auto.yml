# Auto-generated docker-compose for founder-1
version: '3.8'

services:
  asim-founder-1:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        FOUNDER_ID: founder-1
        DEVICE_TYPE: cuda
        MODEL_NAME: gemma-2-2b-it
        GPU_LAYERS: 28
        THREADS: 8
    container_name: asim-founder-1
    restart: unless-stopped
    environment:
      - FOUNDER_ID=founder-1
      - DEVICE_TYPE=cuda
      - MODEL_NAME=gemma-2-2b-it
      - GPU_LAYERS=28
      - THREADS=8
    ports:
      - "8000:8000"
      - "8766:8766"
      - "3000:3000"
    volumes:
      - ./memory:/app/memory
      - ./models:/app/models
      - ./logs:/app/logs
    deploy:
      resources:
        limits:
          cpus: '12.0'
          memory: 15G
        reservations:
          cpus: '6.0'
          memory: 7G
